
name: Generate Snake

on:
  # run automatically once a day
  schedule:
    - cron: "0 0 * * *"
  # let you run it manually from the Actions tab
  workflow_dispatch:
  # also run whenever you push to main
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Generate the snake SVGs
        uses: Platane/snk@v3
        id: snake
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-snake.svg
            dist/github-snake-dark.svg?palette=github-dark
                        dist/github-snake-emerald.svg?color_snake=#c9a44c&color_dots=#0d1512,#14502f,#1d8a57,#3ad98a,#9be8c0
      - name: Push the SVGs to the output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
